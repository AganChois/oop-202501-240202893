# Database Configuration
# Edit file ini untuk mengubah konfigurasi database

# Database Connection
db.url=jdbc:postgresql://localhost:5432/agripos
db.username=postgres
db.password=242424
db.driver=org.postgresql.Driver

# Connection Pool Settings (untuk masa depan)
db.pool.minSize=5
db.pool.maxSize=20
db.pool.timeout=30000

# Application Settings
app.name=AgriPOS
app.version=1.0.0
